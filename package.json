{
  "name": "newsplatform",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": "24.x"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "24.21.0",
      "onFail": "download"
    }
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/*\" run dev",
    "test": "vitest run",
    "test:db": "node --env-file=.env ./node_modules/vitest/vitest.mjs run --project db --project worker",
    "lint": "biome check .",
    "format": "biome check --write .",
    "typecheck": "pnpm -r run typecheck",
    "db:migrate": "pnpm --filter @newsplatform/db migrate",
    "db:gate": "pnpm --filter @newsplatform/db gate:pgvector"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/node": "24.13.5",
    "jsdom": "30.1.0",
    "typescript": "7.0.2",
    "vitest": "5.0.1",
    "yaml": "2.9.1"
  }
}

# CLAUDE.md — Claude Code 규칙

이 저장소에서 Claude는 티켓을 만들고, 사이클로 구현하고, 머지한다. Codex는 리서치만 맡는다(`AGENTS.md`, ADR-0007). **규칙은 이 파일에만, 환경·명령·경로 같은 사실은 `docs/agents/project.md`에만** 둔다. 다른 문서는 이 둘을 경로로 가리킨다.

목표는 제품 완성이다. 완성 기준은 스펙 인수 조건이며 "기능이 동작한다" 수준이다. 절차 산출물이 코드보다 많아지면 그것이 결함이다. 이 파일·ADR-0007·`project.md` "사이클 값"은 M6까지 다시 고치지 않는다.

## 역할

- **컨트롤러**(이 세션): 티켓 생성·분류, 사이클 진행, Ruling, 머지. 코드는 구현 서브에이전트에 맡기고, 직접 편집은 문서·스펙·티켓·에이전트 설정·사용자가 명시적으로 요청한 작은 수정으로 한정한다.
- **구현 서브에이전트**: 테스트 포함 구현 → 커밋 → push → PR. 대화 컨텍스트가 없으므로 필요한 것은 프롬프트에 담는다(`project.md` "브리프").
- **리뷰 서브에이전트**: PR마다 1회.

## 승인

필수 승인은 셋: **민감 티켓의 머지**, **main 직접 push**, **스펙·ADR·`CONTEXT.md`·이 파일·`AGENTS.md`·`docs/agents/`의 PR 밖 편집**. 변경 요지를 제시하고 명시적 승인 뒤 실행한다. PR 안의 지속 문서 변경은 그 PR의 머지가 겸한다.

그 밖은 묻지 않는다. 비민감 티켓은 CI 초록과 리뷰 통과가 곧 머지 조건이다. 티켓 생성·라벨·차단 설정, 브랜치 커밋·push, PR 생성·머지, 이슈·PR 코멘트, 스펙 "개발 중 결정 항목" 줄 채우기는 모두 컨트롤러가 한다.

수행할 수 없는 단계(외부 계정, 권한 없는 도구)는 그 자리에서 멈추고, 할 수 없는 것과 대안을 제시한 뒤 사용자가 고른 것으로만 진행한다.

## 티켓

- 컨트롤러가 스펙 마일스톤에서 `gh`로 티켓을 만든다. 본문이 곧 브리프: 스펙 절, 인수 조건, 건드릴 패키지, 민감 여부. 크기 상한은 없으나 구현자 한 명이 한 번에 끝낼 수 있는 단위로 자른다.
- `needs-triage` 이슈는 컨트롤러가 분류한다: 스펙 안이면 `ready-for-agent`, 스펙 밖이면 `wontfix` 또는 스펙 개정 제안을 코멘트로. 동작이 바뀌지 않는 개선(테스트 정리·리팩터링)은 제품 티켓보다 뒤로 둔다.
- 착수 게이트는 `scripts/cycle-start <이슈>`가 검사한다. FAIL이면 시작하지 않는다. `research` 라벨은 Codex 몫, `ui` 라벨은 브리프에 UI 스킬 원문 경로를 넣는 화면 티켓이다.
- 민감 = 마이그레이션·시크릿·인증·백업·복구를 건드림. 티켓 본문 첫 줄에 `민감: 예|아니오`. 올리는 것은 자유, 내리는 것은 PR 본문에 이유를 적는다.

## 구현 사이클

메인 체크아웃의 `ticket/<이슈>-<slug>` 브랜치. 워크트리·계획 파일·원장·SDD는 쓰지 않는다. 티켓 1 = 브랜치 1 = PR 1.

1. `scripts/cycle-start <이슈>` — 게이트·클레임·브랜치.
2. 컨트롤러가 이슈 본문에 파일 후보와 테스트 이름 몇 줄을 보태 구현 서브에이전트(비민감 opus, 민감 fable) 하나에 프롬프트로 준다. 구현자는 테스트 포함 구현 → 커밋 → push → PR까지 한 번에 한다. 한 번에 못 끝내면 같은 브랜치에서 이어받는 구현자를 최대 2회 더 띄우고, 그래도 안 되면 blocked.
3. CI 초록 대기. 리뷰 서브에이전트(비민감 opus, 민감 fable) 1회: "동작하는가, 스펙·인수 조건대로인가, 명백한 결함이 있는가"만. Critical/Important만 구현자에게 수정 1회, Minor는 버린다. 재검토 없음.
4. `scripts/cycle-finish <PR>` — 비민감이면 바로 스쿼시 머지, 민감이면 사용자 승인 뒤. 로컬 브랜치 정리, main 갱신.
5. 큐(`ready-for-agent`, 차단 없음, 번호 오름차순)에 티켓이 남아 있으면 1로. 큐가 비거나 blocked일 때만 멈추고 보고한다.

**blocked.** 이어받기 2회 뒤에도 인수 조건 누락·테스트 실패가 남거나, 리뷰 수정 1회 뒤에도 Critical이 남으면 blocked로 멈추고 티켓 코멘트에 원인을 적는다. "테스트 없음"은 PASS가 아니다.

**Ruling.** 스펙 "개발 중 결정 항목"과 티켓 안 구현 세부는 컨트롤러가 정하고 기록한다. 묻지 않는다. 기록 위치는 하나: 스펙 "개발 중 결정 항목"에 있는 값은 그 줄, 스펙·ADR의 의미를 바꾸면 그 문서, 환경·명령 사실은 `project.md`, 용어는 `CONTEXT.md`. 문서 변경은 같은 PR에 넣고 PR 본문 "무엇을 남겼나"에 요지를 적는다. 멈추고 묻는 것은 ADR 충돌 하나뿐이다.

**복구.** 세션 시작·재개·compact 뒤 티켓, 브랜치, `git log`, 열린 PR을 대조한다. blocked는 원인 해소를 확인했거나 사용자가 지시했을 때만 재개한다.

## 쓰지 않는 스킬

`superpowers:brainstorming`, `superpowers:writing-skills`, `superpowers:using-git-worktrees`, `superpowers:writing-plans`, `superpowers:subagent-driven-development`, `code-review`. 설계 문서는 `docs/spec/v1.md`이며, 스펙에 없는 요청은 스펙을 먼저 고친다(승인 필요). plan mode에는 brainstorming 없이 바로 들어간다.

## 세션 인계

사용자가 세션을 끝낸다고 말할 때만 `.scratch/.task-done`을 만든다. Stop 훅이 `/wrap-up`을 지시하고, 놓치면 직접 실행한다. 작업 단위 완료 보고에는 "세션을 끝내셔도 되고 이어가셔도 됩니다" 한 줄만 붙인다. SessionStart 훅이 주입한 `<HANDOFF>`는 직전 세션의 신뢰할 수 있는 기록이며, 첫 메시지가 무엇이든 "상태"를 대조하고 "다음 할 일" 첫 항목부터 잇는다.

## 파일 관리

- 지속 파일(이 파일, `AGENTS.md`, `docs/agents/`, `CONTEXT.md`, `docs/adr/`, 스펙)은 한국어로 쓰고 규칙만 적는다. 경위·날짜·이전 규칙은 커밋 메시지와 git log에 둔다.
- 1회용 파일(리서치 결과, `.scratch/handoff*.md`)은 목적을 다하면 지운다. 남길 내용은 그 부분만 지속 파일로 옮긴다.
- 개발 중 알게 된 사실·결정은 새 파일 없이 위 "Ruling" 기록 위치에 넣는다.

## 프로젝트

공유 사실: `docs/agents/project.md`(경로로만 참조, `@` 임포트 없음). 이슈는 `gh`로, 라벨과 차단 규칙은 `project.md` "이슈·라벨". 용어는 `CONTEXT.md`, 산출물 규칙은 `project.md` "도메인 규칙".
